apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bpdm.fullname" . }}-test-connection"
  labels:
    {{- include "bpdm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bpdm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
