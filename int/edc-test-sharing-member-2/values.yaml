################################################################################
# Copyright (c) 2021 Contributors to the Eclipse Foundation
#
# See the NOTICE file(s) distributed with this work for additional
# information regarding copyright ownership.
#
# This program and the accompanying materials are made available under the
# terms of the Apache License, Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0.
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.
#
# SPDX-License-Identifier: Apache-2.0
################################################################################

install:
  postgresql: true
  vault: false

participant:
  # -- Participant Id, resp. the Decentralized IDentifier (DID) of the connector
  id: "did:web:portal-backend.int.catena-x.net:api:administration:staticdata:did:BPNL00000000726A"
  # -- BPNL Number
  bpnl: "BPNL00000000726A"
  # -- Participant Context Id - Newly introduced id for a connector instance (needed for multitenancy)
  contextId: "e0f7de3d-9ecf-4c4f-b570-30a1445e4084"

dcp:
  trustedIssuers:
    - "did:web:portal-backend.int.catena-x.net:api:administration:staticdata:did:BPNL00000003CRHK"
  sts:
    div:
      url: "https://dis-integration-service-prod.eu10.dim.cloud.sap/api/v2.0.0/iatp/catena-x-portal"
    oauth:
      token_url: "https://b74ef27e-2777-4840-bb30-d82871ff1ba5-m6iluaqk.authentication.eu10.hana.ondemand.com/oauth/token"
      client:
        id: "sb-e6c8b75b-675f-4900-b482-d14a08657118!b518219|ica-production-dim-prod-eu10-004-prod-dis-cloud-approuter!b174292"
        secret_alias: "wallet-client-secret"
  # - Configures the Verifiable Presentation cache
  cache:
    # -- Whether the Verifiable Presentation cache is enabled
    enabled: true
    # -- Validity of the Verifiable Presentation cache in seconds
    validity: 86400

controlplane:
  endpoints:
    # -- data management api, used by internal users, can be added to an ingress and must not be internet facing
    management:
      # -- authentication key, must be attached to each 'X-Api-Key' request header
      authKey: <path:bpdm/data/int/edc-test-sharing-member-2/api#management-key>
  bdrs:
    server:
      url: "https://bpn-did-resolution-service.int.catena-x.net/api/directory"
  ## Ingress declaration to expose the network service.
  ingresses:
    ## Public / Internet facing Ingress
    - enabled: true
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
      # -- The hostname to be used to precisely map incoming traffic onto the underlying network service
      hostname: "bpdm-sharing-member-2-edc.int.catena-x.net"
      className: "nginx"
      # -- EDC endpoints exposed by this ingress resource
      endpoints:
        - management
        - control
        - protocol
      # -- TLS [tls class](https://kubernetes.io/docs/concepts/services-networking/ingress/#tls) applied to the ingress resource
      tls:
        # -- Enables TLS on the ingress resource
        enabled: true
        # -- If present overwrites the default secret name
        secretName: tls-secret-edc-test-sharing-member-2-controlplane

dataplane:
  endpoints:
    proxy:
      authKey: <path:bpdm/data/int/edc-test-sharing-member-2/api#proxy-key>
  ## Ingress declaration to expose the network service.
  ingresses:
    ## Public / Internet facing Ingress
    - enabled: true
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
      # -- The hostname to be used to precisely map incoming traffic onto the underlying network service
      hostname: "bpdm-sharing-member-2-edc.int.catena-x.net"
      # -- EDC endpoints exposed by this ingress resource
      endpoints:
        - public
      # -- Defines the [ingress class](https://kubernetes.io/docs/concepts/services-networking/ingress/#ingress-class)  to use
      className: "nginx"
      # -- TLS [tls class](https://kubernetes.io/docs/concepts/services-networking/ingress/#tls) applied to the ingress resource
      tls:
        # -- Enables TLS on the ingress resource
        enabled: true
        # -- If present overwrites the default secret name
        secretName: tls-secret-edc-test-sharing-member-2-dataplane
  token:
    signer:
      privatekey_alias: "token-private-key"
    verifier:
      publickey_alias: "token-public-key"

postgresql:
  jdbcUrl: "jdbc:postgresql://{{ .Release.Name }}-postgresql:5432/edc"
  persistence:
    enabled: true
  auth:
    # -- Credentials of the superuser the chart creates the connector database with.
    password: <path:bpdm/data/int/edc-test-sharing-member-2/postgres#password>
  customUser:
    # -- The database and user the connector itself works with.
    database: "edc"
    name: "user"
    password: <path:bpdm/data/int/edc-test-sharing-member-2/postgres#password>

vault:
  hashicorp:
    url: "https://vault.core.catena-x.net"
    token: <path:bpdm/data/int/edc-test-sharing-member/vault#token>
    paths:
      secret: /v1/bpdm
      folder: "int/edc-test-sharing-member-2"