test:
  v6: "true"
bpdm:
  tasks:
    cron: '-'
  sharingMemberRecords:
    cron: '-'
spring:
  datasource:
    hikari:
      maximum-pool-size: 2
  security:
    oauth2:
      client:
        registration:
          admin:
            provider: "test-keycloak"
            authorization-grant-type: "client_credentials"
            client-id: "BPDM_POOL"
            client-secret: "**********"
          participant:
            provider: "test-keycloak"
            authorization-grant-type: "client_credentials"
            client-id: "BPDM_PARTICIPANT"
            client-secret: "**********"
          sharing-member:
            provider: "test-keycloak"
            authorization-grant-type: "client_credentials"
            client-id: "BPDM_SHARING_MEMBER"
            client-secret: "**********"
          unauthorized:
            provider: "test-keycloak"
            authorization-grant-type: "client_credentials"
            client-id: "BPDM_TASK_CREATOR"
            client-secret: "**********"
