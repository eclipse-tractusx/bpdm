################################################################################
# Copyright (c) 2021 Contributors to the Eclipse Foundation
#
# See the NOTICE file(s) distributed with this work for additional
# information regarding copyright ownership.
#
# This program and the accompanying materials are made available under the
# terms of the Apache License, Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0.
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.
#
# SPDX-License-Identifier: Apache-2.0
################################################################################

name: Lint Charts

on:
  pull_request:
  workflow_call:
    inputs:
      helm_version:
        description: 'helm version to test (default = latest)'
        default: 'latest'
        required: false
        type: string
  workflow_dispatch:
    inputs:
      helm_version:
        description: 'helm version to test (default = latest)'
        default: 'latest'
        required: false
        type: string

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 #v7.0.1
        with:
          fetch-depth: 0

      - name: Check for Release Version
        id: checkRelease
        run: |
          CHART_VERSION=$(grep 'version:' ./charts/bpdm/Chart.yaml | head -n1 | awk '{ print $2}')
          echo "CHART_VERSION: $CHART_VERSION"
          SNAPSHOT=$(echo "$CHART_VERSION" | grep 'SNAPSHOT' || echo "" )
          echo "SNAPSHOT: $SNAPSHOT"
          if [[ -z "$SNAPSHOT" ]]; then 
            IS_RELEASE=true 
          else 
            IS_RELEASE=false 
          fi
          echo "IS_RELEASE: $IS_RELEASE"
          echo "isRelease=$IS_RELEASE" >> $GITHUB_OUTPUT

      - name: Set up Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 #v5.0.1
        with:
          version: ${{ github.event.inputs.helm_version || 'latest' }}

      - name: Set up chart-testing
        uses: helm/chart-testing-action@6ec842c01de15ebb84c8627d2744a0c2f2755c9f #v2.8.0

      - name: Create Chart-Testing Config
        run: |
          cat <<EOF > .chart-testing-config.yaml
          validate-maintainers: false
          check-version-increment: ${{ steps.checkRelease.outputs.isRelease }}
          chart-repos:
            - bitnami=https://charts.bitnami.com/bitnami
          EOF
          echo "cat .chart-testing-config.yaml"
          cat .chart-testing-config.yaml

      - name: Run chart-testing (lint)
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: ct lint --target-branch $DEFAULT_BRANCH --config .chart-testing-config.yaml

      # The second Gate is disabled by default, so no other job renders it: the chart tests deploy it only
      # in their full scope. Rendering it here keeps a change from breaking that topology unnoticed, and
      # costs seconds instead of a second application in a cluster.
      - name: Render the second sharing member's Gate
        run: |
          helm dependency update charts/bpdm
          helm template bpdm-test charts/bpdm --set bpdm-gate-2.enabled=true --set tests.enabled=true > /dev/null
