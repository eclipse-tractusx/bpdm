name: Maven Deploy
on:
  push:
    # only execute when source specific files change
    paths:
      - pom.xml
      - bpdm-common/**
    branches:
      - main
      - rc/**

jobs:
  deploy-maven:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      actions: read
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'adopt'

      #Use Maven package cache
      - name: Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      #Deploy  bpdm-common jar to GitHub Maven registry
      - name: Deploy
        run: ./mvnw -pl bpdm-common,bpdm-pool-api-client deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}